# SABRA pre-training logic audit

## Terminal status

`FULL_SABRA_TRAIN_NOT_AUTHORIZED`

PGM: `SUPPORTED`; Trust: `INCONCLUSIVE`; Need: `SUPPORTED`; Authority: `INCONCLUSIVE`. PCRR: `DROP`.

Need minimum capacity: `C1`. P9 coverage: `1.0`. Stable-but-wrong rows: `522084`.

This report is VisA-only and inference-only; no training, MVTec, or medical evaluation was performed.
