# SABRA Trust-v2 recovery v2 handoff

Scientific recovery completed through VisA and candidate freeze. The preceding invalid artifact collision was preserved at `runs/phase5/sabra/INVALIDATED_TRUST_V2_RUNS/20260820_ARTIFACT_COLLISION/`; its overwritten earlier untracked bytes were not reconstructed.

VisA recovery selected `M1_E_Credibility` with features `E, peer_coherence, query_support_mean, peer_eigen_entropy, stage_query_profile_disagreement`. Corrected M4 is the selected non-PCRR model plus cached `D_rel = abs(PGM_baseline_rank - PCRR_baseline_rank)`. PCRR status is `DROP`. Trust-v2, Need C1, and Authority-v2 are `SUPPORTED`; p16 coverage is `STRONG`.

The candidate freeze was pushed and verified. MVTec external validation was authorized only after that freeze, but no MVTec image root was available. No MVTec image or mask reads, metrics, or confirmatory evidence are claimed. Medical reads are zero. Full SABRA training is not authorized.

Do not rerun Phase2B, rebuild the GT-free cache, alter formulas, retune thresholds, change gates, or use invalid archived artifacts as evidence.
