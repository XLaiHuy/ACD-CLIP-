# Published comparison

The final source-selected RA E16 Medical macro is **87.8904 Pixel AUROC / 37.1625 Pixel AP** under the current exact evaluator.

For provenance, historical H1/H2 and the published ACD-CLIP N=3 comparator are retained as `HISTORICAL_REPORTED` or `PUBLISHED_FROZEN_COMPARATOR`. They are not mixed with the same-current-evaluator rows. The final RA E16 result is below the published `91.55 / 43.03` comparator on both primary pixel metrics.
