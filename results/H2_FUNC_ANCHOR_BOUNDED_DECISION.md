# H2 bounded functional feature-anchor decision

`BOUNDED_TEST_STATUS=FAIL`

The required matched augmented-source-batch invariant failed: all 500 file-name orders match, but image/mask hashes diverge at attempt 0. The candidate teacher construction consumed global RNG before stochastic source augmentation. This makes control/candidate endpoint comparisons non-interpretable.

A second independent violation occurred: the candidate median effective functional/task gradient ratio was 0.617985492383, not an auxiliary 0.10 budget. Control also had one recoverable gradient skip in 500 attempts (0.2%), exceeding the 0.1% validity threshold.

No fixed source evaluation was run because its comparison would be falsely presented as a matched mechanism result. No target evaluation, full training, tuning, additional variant, or retry was run.

`MECHANISM_SCREEN=FAIL`

`FUNCTIONAL_OVERADAPTATION_HYPOTHESIS=STILL_POSSIBLE`

`SELECTED_FEATURE_ANCHOR_MECHANISM=NOT_SUPPORTED`
