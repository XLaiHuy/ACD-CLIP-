# Post-R2 Decision Tree

R2 is preserved as a terminal failure. The diagnostic did not select a
replacement policy.

1. Safety passed, but the downstream R2 gates failed.
2. The post-hoc sign-correct oracle cohort was beneficial; the sign-wrong
   oracle cohort was strongly harmful.
3. No preregistered explanation was `SUPPORTED`; target/ranking and
   spatial/ranking components remain `PLAUSIBLE`.
4. Therefore the correct terminal classification is `MIXED_FAILURE` and the
   program stops for user review.
