# SABRA-CURE Post-R2 Root Cause

Primary: `MIXED_FAILURE`. This is `POST_HOC` diagnostic evidence only; R2
remains `R2_SCIENTIFIC_STOP`.

- H1 wrong-sign target too narrow: `WEAK`
- H2 BOOST/SUPPRESS asymmetry: `WEAK`
- H3 actionability mismatch: `WEAK`
- H4 target-loss/ranking mismatch: `PLAUSIBLE`
- H5 fixed-alpha/subset mismatch: `INSUFFICIENT_EVIDENCE`
- H6 spatial/ranking coupling: `PLAUSIBLE`

The decisive descriptive contrast is oracle-labeled. The persisted R2 action
deployment reduced pAP by 0.437 pp, while `D2_SIGN_CORRECT_ONLY` improved pAP
by 0.603 pp and improved frozen loss. `D3_SIGN_WRONG_ONLY` reduced pAP by
1.816 pp and increased frozen loss. Thus the published wrong-sign controller
substantially reduced risk, but its remaining wrong signs were consequential;
the preregistered H1 retained-harm criterion was not met because the
sign-correct cohort was beneficial rather than harmful.

All oracle cohort statements are `POST_HOC_ORACLE_DIAGNOSTIC`, not deployable
counterfactual results and not a basis to rewrite R2 gates.
