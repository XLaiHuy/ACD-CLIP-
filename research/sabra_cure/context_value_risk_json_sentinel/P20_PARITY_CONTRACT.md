# P20 Parity and Observability Contract

For finite-threshold fixtures P20 must equal P19 exactly. For the `NO_EXPANSION` fixture, in-memory and decoded thresholds must both be positive infinity and produce identical all-false expansion masks for finite `vhat`.

The real checkpoint writer/reader must round-trip a deterministic no-eligible-q fold. A three-fold synthetic parent lifecycle must contain finite, no-expansion, finite folds and retain compact parent ownership, audit/global paths, no parent scientific-array deserialization, and no surviving children.

Each science, audit, global, and global-audit child writes durable stdout and stderr logs plus JSON status containing PID, stage, and exit code. This changes failure observability only.

P19 configuration remains frozen: `INNER_AP_WORKERS=4`; `OMP_NUM_THREADS=1`; `MKL_NUM_THREADS=1`; `OPENBLAS_NUM_THREADS=1`. The performance sanity check must retain AP parity zero and projected science/full runtime limits of 3.5/6.5 hours.
