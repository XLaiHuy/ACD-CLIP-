# R2-v2 Implementation Contract

Implementation is isolated to `tools/sabra_cure/r2v2_harm.py` and its tests.
It must serialize Level-1/Level-2 provenance, parameters, thresholds, scores,
actions, fixed coverage curves, downstream metrics, and independent audit.
The attempt marker rejects any rerun. Historical R0/R1/R2/P10 paths are read
only. Structural feasibility must prove all 22 features, OOF target bounds and
nonzero harm events before the scientific marker is created.
