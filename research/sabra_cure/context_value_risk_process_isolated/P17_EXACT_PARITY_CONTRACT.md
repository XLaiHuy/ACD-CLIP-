# P17 Exact Parity Contract

P17 retains P15's accepted grouped-delta AP engine. Frozen fixture acceptance
is maximum AP error `0.0` where achievable (otherwise documented <= `1e-12`),
with speedup >= 5x over P14 reference. No quantization, approximation, target,
feature, tie-policy, or model change is permitted.
